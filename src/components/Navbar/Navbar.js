import React from "react";
import { Nav } from "./Navbar.elements"; // Import the styles for component Nav

const Navbar = () => {
  return (
    <>
      <Nav>Test</Nav>
    </>
  );
};

export default Navbar;
